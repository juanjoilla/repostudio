# === CONFIGURACIÓN PARA DESARROLLO CON POSTGRESQL (RENDER) ===

# Base de datos PostgreSQL en Render
spring.datasource.url=jdbc:postgresql://dpg-d5s2pksoud1c73900lcg-a.oregon-postgres.render.com/artesluis_db
spring.datasource.username=artesluis_db_user
spring.datasource.password=7fdqD8DhIwfSbpOpaqxSoc3r3RUzgp8z
spring.datasource.driver-class-name=org.postgresql.Driver

# === CONFIGURACIÓN DE HIBERNATE / JPA ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# === SSL PARA RENDER ===
spring.datasource.hikari.connection-test-query=SELECT 1

# === CONFIGURACIÓN DE DATOS INICIALES ===
# Ejecutar data.sql al inicio (solo en desarrollo)
spring.sql.init.mode=always
spring.sql.init.data-locations=classpath:data.sql
spring.sql.init.continue-on-error=true

# === CONFIGURACIÓN DE LOGS PARA DESARROLLO ===
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.tool.hbm2ddl=debug
logging.level.org.springframework.web=info
logging.level.com.artesluis=debug
logging.level.org.springframework.jdbc.core.JdbcTemplate=debug

# === CONFIGURACIÓN DE ARCHIVOS ESTÁTICOS PARA DESARROLLO ===
spring.web.resources.static-locations=classpath:/static/,classpath:/public/,file:uploads/
spring.web.resources.cache.cachecontrol.max-age=0
spring.mvc.static-path-pattern=/static/**

# === CONFIGURACIÓN DE CORS PARA DESARROLLO ===
spring.web.cors.allowed-origins=http://localhost:3000,http://localhost:8080
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true